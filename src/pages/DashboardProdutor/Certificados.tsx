import React from "react";
import { Card } from "@/components/ui/card";

const CertificadosProdutor = () => {
  return (
    <div className="space-y-6">
      <div>
        <h1 className="text-3xl font-bold text-foreground">Certificados — Produtor</h1>
        <p className="text-muted-foreground">Página para upload e gerenciamento de certificados sanitários (em branco)</p>
      </div>

      <Card className="p-6">
        <p className="text-sm text-muted-foreground">Conteúdo a ser implementado.</p>
      </Card>
    </div>
  );
};

export default CertificadosProdutor;
